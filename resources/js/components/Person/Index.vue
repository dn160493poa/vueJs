<template>
    <div class="w-50">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Age</th>
                <th scope="col">Job</th>
                <th scope="col">Edit</th>
                <th scope="col">Delete</th>
            </tr>
            </thead>
            <tbody>
            <tr v-for="person in people">
                <td>
                    <router-link :to="{ name: 'person.edit', params: { 'id': person.id }}">{{ person.name }}</router-link>
                </td>
                <td>{{ person.age }}</td>
                <td>{{ person.job }}</td>
                <td>
                    <router-link :to="{ name: 'person.edit', params: { 'id': person.id }}">Edit</router-link>
                </td>
                <td>
                    <input @click.prevent="$store.dispatch('deletePerson', person.id)" type="submit" value="Delete" class="btn btn-primary">
                </td>
            </tr>
            </tbody>
        </table>
    </div>
</template>

<script>
export default {
    name: "Index",

    mounted() {
        this.$store.dispatch('getPeople')
        this
    },

    methods: {
    },

    computed: {
        people(){
            return this.$store.getters.people
        }
    }


}
</script>

<style scoped>

</style>
